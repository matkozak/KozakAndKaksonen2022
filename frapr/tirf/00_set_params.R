rm(list = ls())

#param_rm <- 1 # how many frames to additionally remove after bleach
param_frame <- 0.53 # frame length in seconds
param_pre <- 5 # how much pre-bleach time to average, in seconds
param_fit <- c(0,60) # start and end time for fitting

# which events to average (event # in full experiment list)
param_select <- c()

